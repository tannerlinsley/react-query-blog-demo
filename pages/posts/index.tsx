import React, { FC } from 'react';
import { Layout } from '../../src/components/layout';
import Posts from '../../src/screens/blog';

const BlogPosts: FC = () => (
  <Layout>
    <Posts />
  </Layout>
);

export default BlogPosts;
